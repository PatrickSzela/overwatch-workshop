import './menuItem.del';

class Text: MenuItem 
{
  public constructor(
    in String text! = ' '
  ) {
    TextConstructor(text);
  }

  protected void TextConstructor(
    String text! = ' '
  ) "[Text] Constructor" {
    MenuItemConstructor(text, null);
    
    this.disabled = true;
  }
}