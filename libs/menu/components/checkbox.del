import './toggle.del';

class Checkbox {
  public static Component New(
    in String label, 
    in (() => Boolean) defaultChecked = null,
    in Number childIndex = null
  ): {
    ..Toggle.New(label, defaultChecked, childIndex)
  };

  // public override void Render() "[Checkbox] Render" {
  //   onRender(this);
  //   renderedText = $"{checked ? "[✓]" : "[    ]"}​ {child.renderedText || text}";
  // }
}