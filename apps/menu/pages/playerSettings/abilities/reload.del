import './_helpers.del';
import './_index.del';

String PAGE_ABILITIES_RELOAD_ID: PAGE_ABILITIES_ID + 'reload';
String PAGE_ABILITIES_RELOAD_NAME: 'Reload';

rule: 'Player Settings/Abilities/Reload'
Event.OngoingPlayer
if(menuState.pageId == PAGE_ABILITIES_RELOAD_ID)
{
  PageAbilitiesRuleGenerator(PAGE_ABILITIES_RELOAD_NAME, Button.Reload);
}
