import './_helpers.del';
import './_index.del';

String PAGE_ABILITIES_PRIMARY_FIRE_ID: PAGE_ABILITIES_ID + 'primaryFire';
String PAGE_ABILITIES_PRIMARY_FIRE_NAME: 'Primary Fire';

rule: 'Player Settings/Abilities/Primary Fire'
Event.OngoingPlayer
if(menuState.pageId == PAGE_ABILITIES_PRIMARY_FIRE_ID)
{
  PageAbilitiesRuleGenerator(PAGE_ABILITIES_PRIMARY_FIRE_NAME, Button.PrimaryFire);
}
