settings
{
  lobby 
  {
    Return To Lobby: Never
  }
  modes
  {
    General
    {
      Competitive Rules: On
    }
    Clash Stadium
    Control Stadium
    Payload Race Stadium
    Push Stadium
  }
  main
  {
    Mode Name: "{{manifest.name}}"
    Description: "Have you ever wanted to play a Stadium game with heroes that are not enabled in it by default, against a team of either Stadium enabled heroes or non-Stadium ones? Well, now you can!

----------
Version: {{manifest.version}}
Import Code: {{manifest.code}}
Created by: {{manifest.author.battletag}}"
  }
}